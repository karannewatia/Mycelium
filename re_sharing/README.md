This is a simple proof-of-concept implementation of the extended VSR protocol.

Requirements:
- python3
- lagrange (can be installed using `pip install lagrange`)

Run `python vsr.py` to see the output of the protocol.
